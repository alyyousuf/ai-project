# AI Project

Minimal scaffold for an AI Project — Next.js + Tailwind + simple in-memory project store.

Quick start:

1. npm install
2. npm run dev

API:
- GET /api/projects — list projects
- POST /api/projects — create project
# ai-project